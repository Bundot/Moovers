import { View, ScrollView } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
import Txt from "../components/Txt";

export default function MoversTracker(){
    return(
        <SafeAreaView>
            <View>
                <Txt>Hello</Txt>
            </View>
        </SafeAreaView>
    )
}